# StockPricePrediction-NN

This project uses an artificial recurrent neural network called "Long Short Term Memory" aslo known as LSTM to predict the closing stock price of a corporation in this case Apple Inc. using the past 60 day stock price.